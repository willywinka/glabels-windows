diff --git a/glabels/ui/ObjectEditor.ui b/glabels/ui/ObjectEditor.ui
--- a/glabels/ui/ObjectEditor.ui
+++ b/glabels/ui/ObjectEditor.ui
@@ -683,6 +683,100 @@
                </item>
               </layout>
              </item>
+             <item row="4" column="0">
+              <widget class="QLabel" name="barcodeTextFontFamilyLabel">
+               <property name="text">
+                <string>Text font:</string>
+               </property>
+              </widget>
+             </item>
+             <item row="4" column="1">
+              <widget class="QFontComboBox" name="barcodeTextFontFamilyCombo"/>
+             </item>
+             <item row="5" column="0">
+              <widget class="QLabel" name="barcodeTextFontSizeLabel">
+               <property name="text">
+                <string>Text size:</string>
+               </property>
+              </widget>
+             </item>
+             <item row="5" column="1">
+              <widget class="QDoubleSpinBox" name="barcodeTextFontSizeSpin">
+               <property name="decimals">
+                <number>1</number>
+               </property>
+               <property name="minimum">
+                <double>1.000000000000000</double>
+               </property>
+               <property name="maximum">
+                <double>200.000000000000000</double>
+               </property>
+               <property name="singleStep">
+                <double>0.500000000000000</double>
+               </property>
+               <property name="suffix">
+                <string> pt</string>
+               </property>
+              </widget>
+             </item>
+             <item row="6" column="0">
+              <widget class="QLabel" name="barcodeTextStyleLabel">
+               <property name="text">
+                <string>Text style:</string>
+               </property>
+              </widget>
+             </item>
+             <item row="6" column="1">
+              <layout class="QHBoxLayout" name="barcodeTextStyleLayout">
+               <item>
+                <widget class="QCheckBox" name="barcodeTextBoldCheck">
+                 <property name="text">
+                  <string>Bold</string>
+                 </property>
+                </widget>
+               </item>
+               <item>
+                <widget class="QCheckBox" name="barcodeTextItalicCheck">
+                 <property name="text">
+                  <string>Italic</string>
+                 </property>
+                </widget>
+               </item>
+               <item>
+                <widget class="QCheckBox" name="barcodeTextUnderlineCheck">
+                 <property name="text">
+                  <string>Underline</string>
+                 </property>
+                </widget>
+               </item>
+              </layout>
+             </item>
+             <item row="7" column="0">
+              <widget class="QLabel" name="barcodeTextAlignLabel">
+               <property name="text">
+                <string>Text alignment:</string>
+               </property>
+              </widget>
+             </item>
+             <item row="7" column="1">
+              <widget class="QComboBox" name="barcodeTextAlignCombo">
+               <item>
+                <property name="text">
+                 <string>Left</string>
+                </property>
+               </item>
+               <item>
+                <property name="text">
+                 <string>Center</string>
+                </property>
+               </item>
+               <item>
+                <property name="text">
+                 <string>Right</string>
+                </property>
+               </item>
+              </widget>
+             </item>
              <item row="0" column="1">
               <widget class="glabels::BarcodeMenuButton" name="barcodeStyleButton"/>
              </item>
diff --git a/glabels/ObjectEditor.cpp b/glabels/ObjectEditor.cpp
--- a/glabels/ObjectEditor.cpp
+++ b/glabels/ObjectEditor.cpp
@@ -62,6 +62,20 @@
                 textColorButton->init( tr("Default"), QColor(0,0,0,255), QColor(0,0,0,255) );
                 barcodeColorButton->init( tr("Default"), QColor(0,0,0,255), QColor(0,0,0,255) );
                 shadowColorButton->init( tr("Default"), QColor(0,0,0,255), QColor(0,0,0,255) );
+
+                connect( barcodeTextFontFamilyCombo, SIGNAL(currentFontChanged(QFont)),
+                         this, SLOT(onBarcodeControlsChanged()) );
+                connect( barcodeTextFontSizeSpin, SIGNAL(valueChanged(double)),
+                         this, SLOT(onBarcodeControlsChanged()) );
+                connect( barcodeTextBoldCheck, SIGNAL(toggled(bool)),
+                         this, SLOT(onBarcodeControlsChanged()) );
+                connect( barcodeTextItalicCheck, SIGNAL(toggled(bool)),
+                         this, SLOT(onBarcodeControlsChanged()) );
+                connect( barcodeTextUnderlineCheck, SIGNAL(toggled(bool)),
+                         this, SLOT(onBarcodeControlsChanged()) );
+                connect( barcodeTextAlignCombo, SIGNAL(currentIndexChanged(int)),
+                         this, SLOT(onBarcodeControlsChanged()) );
+
                 textInsertFieldButton->setText( tr("Insert substitution field") );
                 barcodeInsertFieldButton->setText( tr("Insert substitution field") );
                 imageFieldButton->setText( tr("Use substitution field") );
@@ -258,6 +258,32 @@
                         barcodeShowTextCheck->setChecked( textFlag );
                         barcodeChecksumCheck->setChecked( csFlag );
                         barcodeColorButton->setColorNode( mObject->bcColorNode() );
+
+                        barcodeTextFontFamilyCombo->setCurrentFont( QFont(mObject->fontFamily()) );
+                        barcodeTextFontSizeSpin->setValue( mObject->fontSize() );
+                        barcodeTextBoldCheck->setChecked( mObject->fontWeight() == QFont::Bold );
+                        barcodeTextItalicCheck->setChecked( mObject->fontItalicFlag() );
+                        barcodeTextUnderlineCheck->setChecked( mObject->fontUnderlineFlag() );
+
+                        int alignIndex = 1;
+                        if ( mObject->textHAlign() == Qt::AlignLeft )
+                        {
+                                alignIndex = 0;
+                        }
+                        else if ( mObject->textHAlign() == Qt::AlignRight )
+                        {
+                                alignIndex = 2;
+                        }
+                        barcodeTextAlignCombo->setCurrentIndex( alignIndex );
+
+                        const bool textControlsEnabled = bcStyle.canText() && textFlag;
+                        barcodeTextFontFamilyCombo->setEnabled( textControlsEnabled );
+                        barcodeTextFontSizeSpin->setEnabled( textControlsEnabled );
+                        barcodeTextBoldCheck->setEnabled( textControlsEnabled );
+                        barcodeTextItalicCheck->setEnabled( textControlsEnabled );
+                        barcodeTextUnderlineCheck->setEnabled( textControlsEnabled );
+                        barcodeTextAlignCombo->setEnabled( textControlsEnabled );
+
                         barcodeDataEdit->setText( mObject->bcData() );
 
                         mBlocked = false;
@@ -696,6 +696,32 @@
                         mObject->setBcTextFlag( textFlag );
                         mObject->setBcChecksumFlag( csFlag );
                         mObject->setBcColorNode( barcodeColorButton->colorNode() );
+
+                        mObject->setFontFamily( barcodeTextFontFamilyCombo->currentFont().family() );
+                        mObject->setFontSize( barcodeTextFontSizeSpin->value() );
+                        mObject->setFontWeight( barcodeTextBoldCheck->isChecked() ? QFont::Bold : QFont::Normal );
+                        mObject->setFontItalicFlag( barcodeTextItalicCheck->isChecked() );
+                        mObject->setFontUnderlineFlag( barcodeTextUnderlineCheck->isChecked() );
+
+                        Qt::Alignment textAlign = Qt::AlignHCenter;
+                        if ( barcodeTextAlignCombo->currentIndex() == 0 )
+                        {
+                                textAlign = Qt::AlignLeft;
+                        }
+                        else if ( barcodeTextAlignCombo->currentIndex() == 2 )
+                        {
+                                textAlign = Qt::AlignRight;
+                        }
+                        mObject->setTextHAlign( textAlign );
+
+                        const bool textControlsEnabled = bcStyle.canText() && textFlag;
+                        barcodeTextFontFamilyCombo->setEnabled( textControlsEnabled );
+                        barcodeTextFontSizeSpin->setEnabled( textControlsEnabled );
+                        barcodeTextBoldCheck->setEnabled( textControlsEnabled );
+                        barcodeTextItalicCheck->setEnabled( textControlsEnabled );
+                        barcodeTextUnderlineCheck->setEnabled( textControlsEnabled );
+                        barcodeTextAlignCombo->setEnabled( textControlsEnabled );
+
                         mObject->setBcData( barcodeDataEdit->toPlainText() );
 
                         mBlocked = false;
diff --git a/model/ModelBarcodeObject.cpp b/model/ModelBarcodeObject.cpp
--- a/model/ModelBarcodeObject.cpp
+++ b/model/ModelBarcodeObject.cpp
@@ -119,4 +119,10 @@
                 mBcFormatDigits = object->mBcFormatDigits;
                 mBcData         = object->mBcData;
                 mBcColorNode    = object->mBcColorNode;
+                mTextFontFamily        = object->mTextFontFamily;
+                mTextFontSize          = object->mTextFontSize;
+                mTextFontWeight        = object->mTextFontWeight;
+                mTextFontItalicFlag    = object->mTextFontItalicFlag;
+                mTextFontUnderlineFlag = object->mTextFontUnderlineFlag;
+                mTextHAlign            = object->mTextHAlign;
                 update(); // Initialize cached editor layouts
diff --git a/model/XmlLabelCreator.cpp b/model/XmlLabelCreator.cpp
--- a/model/XmlLabelCreator.cpp
+++ b/model/XmlLabelCreator.cpp
@@ -283,4 +283,10 @@
                 XmlUtil::setStringAttr( node, "style", object->bcStyle().id() );
                 XmlUtil::setBoolAttr( node, "text", object->bcTextFlag() );
                 XmlUtil::setBoolAttr( node, "checksum", object->bcChecksumFlag() );
+                XmlUtil::setStringAttr( node, "text_font_family", object->fontFamily() );
+                XmlUtil::setDoubleAttr( node, "text_font_size", object->fontSize() );
+                XmlUtil::setWeightAttr( node, "text_font_weight", object->fontWeight() );
+                XmlUtil::setBoolAttr( node, "text_font_italic", object->fontItalicFlag() );
+                XmlUtil::setBoolAttr( node, "text_font_underline", object->fontUnderlineFlag() );
+                XmlUtil::setAlignmentAttr( node, "text_align", object->textHAlign() );
                 if ( object->bcColorNode().isField() )
diff --git a/model/XmlLabelParser.cpp b/model/XmlLabelParser.cpp
--- a/model/XmlLabelParser.cpp
+++ b/model/XmlLabelParser.cpp
@@ -563,2 +563,10 @@
                 QString bcData = XmlUtil::getStringAttr( node, "data", "" );
+
+                QString       textFontFamily        = XmlUtil::getStringAttr( node, "text_font_family", "Sans" );
+                double        textFontSize          = XmlUtil::getDoubleAttr( node, "text_font_size", 10.0 );
+                QFont::Weight textFontWeight        = XmlUtil::getWeightAttr( node, "text_font_weight", QFont::Normal );
+                bool          textFontItalicFlag    = XmlUtil::getBoolAttr( node, "text_font_italic", false );
+                bool          textFontUnderlineFlag = XmlUtil::getBoolAttr( node, "text_font_underline", false );
+                Qt::Alignment textHAlign            = XmlUtil::getAlignmentAttr( node, "text_align", Qt::AlignHCenter );
+
                 /* affine attrs */
@@ -572,3 +580,10 @@
-                return new ModelBarcodeObject( x0, y0, w, h, lockAspectRatio,
-                                               bcStyle, bcTextFlag, bcChecksumFlag, bcData, bcColorNode,
-                                               QTransform( a[0], a[1], a[2], a[3], a[4], a[5] ) );
+                auto* object = new ModelBarcodeObject( x0, y0, w, h, lockAspectRatio,
+                                                       bcStyle, bcTextFlag, bcChecksumFlag, bcData, bcColorNode,
+                                                       QTransform( a[0], a[1], a[2], a[3], a[4], a[5] ) );
+                object->setFontFamily( textFontFamily );
+                object->setFontSize( textFontSize );
+                object->setFontWeight( textFontWeight );
+                object->setFontItalicFlag( textFontItalicFlag );
+                object->setFontUnderlineFlag( textFontUnderlineFlag );
+                object->setTextHAlign( textHAlign );
+                return object;
